info:
  name: 13.getUsersAsMember
  type: http
  seq: 15

http:
  method: GET
  url: 127.0.0.1:8000/api/v1/users/
  auth:
    type: bearer
    token: "{{jwt_access}}"

runtime:
  scripts:
    - type: before-request
      code: |-
        const axios = require('axios');

        const response = await axios.post(
          'http://127.0.0.1:8000/api/v1/users/login/',{
            email: 'testMember@test.com',
            password: 'testMember'
          }
        );
        bru.setVar("jwt_access", response.data.access);
    - type: after-response
      code: |-
        const data = res.getBody();
        const users = data.results || data;
        const adminUser = users.find((user) => user.fullName === 'SuperAdmin');
        const memberUser = users.find((user) => user.fullName === 'Member');

        if (!adminUser || adminUser.email !== undefined) {
          throw new Error('Member should not see admin email');
        }
        if (!memberUser || memberUser.email !== 'testMember@test.com') {
          throw new Error('Member should see own email');
        }

        bru.setVar("jwt_access", null);
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
