opencollection: 1.0.0

info:
  name: nexus_integration_test
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ""
      port: ""
      auth:
        username: ""
        password: ""
      bypassProxy: ""

request:
  auth:
    type: bearer
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
