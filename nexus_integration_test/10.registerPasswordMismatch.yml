info:
  name: 10.registerPasswordMismatch
  type: http
  seq: 12

http:
  method: POST
  url: 127.0.0.1:8000/api/v1/users/register/
  body:
    type: json
    data: |-
      {
        "email": "test-mismatch@test.com",
        "password": "dkizxs32c",
        "password_confirm": "dkizxs32d"
      }

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "400"
    - expression: res.body.passwordConfirm
      operator: isNotEmpty

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
